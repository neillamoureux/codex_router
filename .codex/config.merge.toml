# The primary interactive session acts as:
# - the complexity classifier
# - the coordinator
# - the implementation agent for simple tasks
model = "gpt-5.6-luna"
model_reasoning_effort = "low"

[agents]
enabled = true
max_concurrent_threads_per_session = 2
