# Install on OpenCode

Add [INSTRUCTIONS.md](INSTRUCTIONS.md) to the project-level instruction file or
configuration supported by the target OpenCode installation. Preserve existing
instructions and merge the routing rules.

Keep the shared `docs/` directory in the project. Configure provider/model
names in OpenCode's own configuration; this package intentionally contains no
credentials, runtime adapter, or universal model mapping.
